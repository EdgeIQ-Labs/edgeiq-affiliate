{
  "name": "relay",
  "private": true,
  "version": "0.1.0",
  "description": "Relay - Self-hosted affiliate marketing tool by EdgeIQ Labs",
  "scripts": {
    "dev:api": "pnpm --filter @relay/api dev",
    "dev:web": "pnpm --filter @relay/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @relay/core db:generate",
    "db:migrate": "pnpm --filter @relay/core db:migrate",
    "seed:edgeiq": "bun run scripts/seed-edgeiq.ts"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "license": "AGPL-3.0-or-later",
  "author": "EdgeIQ Labs",
  "homepage": "https://edgeiqlabs.com/affiliate",
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.13"
  }
}
